# Required Configuration
ANTHROPIC_API_KEY=your_api_key_here

# Optional Database Configuration
DB_HOST=localhost          # Optional: Set to enable database functionality
DB_NAME=your_database     # Required if DB_HOST is set
DB_USER=your_username     # Required if DB_HOST is set
DB_PASSWORD=your_password # Required if DB_HOST is set

# Optional Directory Configuration
LOG_DIR=.session_logs     # Optional: Default shown
EDITOR_DIR=editor_dir     # Optional: Default shown
